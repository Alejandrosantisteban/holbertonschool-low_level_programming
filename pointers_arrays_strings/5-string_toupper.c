#include "main.h"

/**
 * string_toupper - changes all lowecase letters to uppercase
x* @str: pointer to a string
 *
 * Return: char
 */
char *string_toupper(char *str)
{
	int x;

	for (x = 0; str[x] != '\0'; x++)
	{
		if (str[x] >= 97 && str[x] <= 122)
			str[i] = str[x] - 32;
	}
	return (a);
}
